spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${mysql.url}
    username: ${mysql.username}
    password: ${mysql.password}
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org:
      hibernate:
        orm:
          jdbc:
            bind: trace
        SQL: debug
        type: trace
      springframework:
        security: debug
        web.socket: debug
        messaging: debug


jwt:
  secret: NiggaNiggaNiggaNiggaNiggaNiggaNiggaImOneHundredPercentNigga